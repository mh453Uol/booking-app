@model BarberBooking.Models.ViewModels.BookingViewModel

<h3 class="text-center p-2">2. Select a service</h3>

    @foreach(var service in Model.Services) {
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-grow-1">
                        <h5 class="card-title">@service.Name</h5>
                        <h6 class="card-subtitle mb-2 text-muted">⌚ @service.Duration 💰 @service.Price</h6>
                    </div>
                    <a class="btn btn-primary" role="button" asp-controller="booking" asp-route-resourceId="@Model.ResourceId" asp-route-servicesId="@service.Id">Next</a>
                </div>
            </div>
        </div>
    }
